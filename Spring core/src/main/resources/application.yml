spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/java_task
    username: andrii
    password: lmc325
    driver-class-name: org.postgresql.Driver